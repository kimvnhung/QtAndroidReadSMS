class QtAndroidService {
    SLOT(void requestServiceStatus());
    SLOT(void requestBackground(const QString &action, const QString &data));

    SIGNAL(sendToUI(const QString &action, const QString &data));
    SIGNAL(serviceStatusChanged(bool isConnected));
}
