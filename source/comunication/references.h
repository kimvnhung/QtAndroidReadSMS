#ifndef REFERENCES_H
#define REFERENCES_H

#include <QObject>

enum ToastDuration{
    SHORT = 0,
    LONG = 1
};

const QString DATABASE_PREFIX = "DATABASE_AVAILABLE";
const QString UPDATE_DATA = "UPDATE_DATA";
const QString INITIALIZE_SERVICE = "init_service";


#endif // REFERENCES_H
